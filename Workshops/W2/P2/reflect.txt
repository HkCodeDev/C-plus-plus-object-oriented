/*****************************************************************************
                  Workshop - #2 (Part - 2)>
Full Name : Hoda Karimi
Student ID# : 138611223
 Email : hkarimi9@myseneca.ca
 Section : NAA
 File: reflect.txt
 Date:
    Authenticity Declaration :
I declare this submission is the result of my own work and has not been
shared with any other student or 3rd party content provider.This submitted
piece of work is entirely of my own creation.
* ****************************************************************************/
the reason for the significant time difference between the copy and move operations:
As mentioned before in class notes, move is all about switching 
addresses, transfering the ownership to another
instead of copying, and this is much faster compared to copying method.
which mean move assignment operator skips the duplication part.
If we take a closer look at both of them:
// Copy assignment operator: 
 ProteinDatabase& ProteinDatabase::operator=(const ProteinDatabase& other) {
     // 1. Check for self-assignment
     if (this != &other) {
         // 2. Clean-up existing resources
         delete[] sequences;
         // Clear the pointer after deleting to avoid dangling references
         sequences = nullptr; 

         // 3. Shallow copy of non-resource data
         numSequences = other.numSequences;

         // 4. Deep copy of the resource
         if (numSequences > 0) {
             // Allocate new memory for sequences
             sequences = new std::string[numSequences];  
             for (size_t i = 0; i < numSequences; ++i) {
                 sequences[i] = other.sequences[i];  // Copy each sequence
             }
         }
         else {
             sequences = nullptr;  // No sequences to copy
         }
     }
     return *this;
 }
// Move assignment operator
ProteinDatabase& ProteinDatabase::operator=(ProteinDatabase&& other) noexcept {
    if (this != &other) {
        delete[] sequences;
        sequences = other.sequences;
        numSequences = other.numSequences;
        other.sequences = nullptr;
        other.numSequences = 0;
    }
    return *this;
}
as you can see, move assignment operator doesn't have the fourth
process of copy assignment, which is deep copy.
In deep copying, we need to allocate new memory and
going through each element, that takes time.
I also used std::move in the Constructor to load protein sequences from a file
to transfer the string to newSequence array:
for (size_t i = 0; i < allocatedSize; ++i) {
    newSequences[i] = std::move(tempSequences[i]);
}
I used it to avoid additional memory allocation
because in that case, we would need to call the std:string's
copy constructor and copy assignment, especially now
that we have a considerably large data in our hand(proteinsequences.txt)
I could just use this instead of move: newSequences[i] = tempSequences[i];
But If I do that, I will get a warning in w2p2.cpp
Another new thing I learned was about <chrono> library.
to use this std::chrono::duration_cast<std::chrono::nanoseconds>(endTime - startTime);
we need end time and start time.
this is a template function that converts the duration
to nanoseconds, and stored it to task struct.
